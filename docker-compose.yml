services:
  mcp-server:
    build: .
    ports:
      - "8080:8080"
    environment:
      - MCP_TRANSPORT=sse
      - MCP_PORT=8080
    volumes:
      - ./instructions.yaml:/app/instructions.yaml:ro
    restart: unless-stopped
