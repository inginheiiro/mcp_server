# MCP Instructions Server - Environment Variables
# Copy this file to .env and adjust values as needed

# Transport mode: "stdio" (default) or "sse" (HTTP/SSE for remote access)
MCP_TRANSPORT=sse

# Port for SSE transport (ignored in stdio mode)
MCP_PORT=8080

# Alternative: Railway/Heroku style port
# PORT=8080
