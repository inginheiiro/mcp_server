# MCP Instructions Server - Environment Variables
# Copy this file to .env and adjust values as needed

# Transport mode:
#   "stdio"           - Default, for local/direct Python execution
#   "streamable-http" - Recommended for remote/HTTP access (more stable)
#   "sse"             - Legacy HTTP/SSE transport
MCP_TRANSPORT=streamable-http

# Port for HTTP transports (ignored in stdio mode)
MCP_PORT=8080

# Alternative: Railway/Heroku style port
# PORT=8080
